query GetNextAiringAnimeEpisode(
  $idMal: Int!
) {
    Media(idMal: $idMal, type: ANIME) {
      id
      idMal
      nextAiringEpisode {
        id
        episode
        timeUntilAiring
        airingAt
      }
    }
}